---
phase: 02-redesign-social-proof-conversion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/testimonials-section-simple.tsx
  - components/contact-section-simple.tsx
  - app/globals.css
autonomous: true
requirements:
  - DSGN-01

must_haves:
  truths:
    - "testimonials-section-simple.tsx has zero hover:shadow-lg occurrences"
    - "testimonials-section-simple.tsx has zero shadcn Card/Badge imports"
    - "testimonials-section-simple.tsx uses only font-black or font-normal weights"
    - "contact-section-simple.tsx uses trackEvent instead of direct posthog.capture"
    - "globals.css .gradient-hover does not use shadow-xl"
  artifacts:
    - path: "components/testimonials-section-simple.tsx"
      provides: "Monolithe-compliant testimonial cards without shadcn"
      contains: "font-black"
    - path: "components/contact-section-simple.tsx"
      provides: "Normalized analytics events"
      contains: "trackEvent"
    - path: "app/globals.css"
      provides: "Clean CSS without shadow-xl in gradient-hover"
  key_links:
    - from: "components/contact-section-simple.tsx"
      to: "lib/analytics/track-event.ts"
      via: "import trackEvent"
      pattern: "import.*trackEvent.*from.*@/lib/analytics/track-event"
---

<objective>
Audit and fix all Design System Monolithe violations in files that Phase 2 will touch, per D-11. This cleans the foundation before building new components.

Purpose: D-11 mandates a dedicated fix pass BEFORE social proof and CTA work. Fixing violations first prevents cascading them into new code.
Output: Clean testimonials section (no shadcn, no shadow-lg, correct font weights), normalized ContactSectionSimple analytics, clean globals.css.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-redesign-social-proof-conversion/02-UI-SPEC.md
@.planning/phases/02-redesign-social-proof-conversion/02-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From lib/analytics/track-event.ts:
```typescript
import posthog from 'posthog-js'
import type { AnalyticsEventName, EventPayload } from './types'

export function trackEvent(event: AnalyticsEventName, payload: EventPayload): void
```

From lib/analytics/types.ts:
```typescript
export type AnalyticsEventName = 'cta_click' | 'phone_click' | 'form_submit'

export interface EventPayload {
  page: string
  element_id: string
  element_text: string
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix Monolithe violations in testimonials-section-simple.tsx</name>
  <files>components/testimonials-section-simple.tsx</files>
  <read_first>
    - components/testimonials-section-simple.tsx
    - app/globals.css
    - docs/Design.md
    - .planning/phases/02-redesign-social-proof-conversion/02-UI-SPEC.md (C-01 section)
  </read_first>
  <behavior>
    - Test 1: Component does NOT import from @/components/ui/card
    - Test 2: Component does NOT import from @/components/ui/badge
    - Test 3: Component output does NOT contain hover:shadow-lg
    - Test 4: Component output does NOT contain font-semibold or font-bold
    - Test 5: Component output contains font-black for author name
    - Test 6: Component renders 3 testimonial cards with expected content
  </behavior>
  <action>
    Per D-11 and DSGN-01, refactor testimonials-section-simple.tsx to remove ALL Design System violations:

    1. **Remove shadcn imports**: Delete `import { Card, CardContent } from "@/components/ui/card"` and `import { Badge } from "@/components/ui/badge"`.

    2. **Replace Card/CardContent with plain divs**: Each testimonial card becomes:
    ```tsx
    <div className="h-full bg-white border border-gray-200 p-6">
    ```
    No hover effect, no shadow -- static cards per UI-SPEC C-01.

    3. **Replace Badge with micro-label span**: The location badge becomes:
    ```tsx
    <span className="text-xs font-black uppercase tracking-[0.3em] text-gray-secondary">
      {testimonial.location}
    </span>
    ```

    4. **Fix font weights**:
    - Line 84 `font-semibold` on author name -> `font-black`
    - Line 87 `font-medium` on role -> `font-black` (per UI-SPEC: "Author role label: 14px, 900, text-red-primary")
    - Line 46 `font-bold` on h2 -> `font-black`
    - Line 58 `font-bold` on h3 -> `font-black`

    5. **Fix h2 heading typography** per UI-SPEC: `text-3xl md:text-4xl` -> `text-4xl` with `tracking-[-0.04em] leading-[0.95]`

    6. **Remove the h3 "Temoignages clients" subheading** (not in UI-SPEC C-01 design -- section header is sufficient).

    7. **Remove the wrapping div around testimonials grid** (the one with className="mb-16" at line 57). The grid goes directly after the header div.

    Keep the component as `'use client'` (it was already), keep all existing data intact, keep star ratings with lni-star-filled.
  </action>
  <verify>
    <automated>cd /Users/alban/Developer/e2ivoip-front && npx jest tests/components/testimonials-section-simple.test.tsx --no-cache 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "from.*@/components/ui/card" components/testimonials-section-simple.tsx returns 0
    - grep -c "from.*@/components/ui/badge" components/testimonials-section-simple.tsx returns 0
    - grep -c "hover:shadow-lg" components/testimonials-section-simple.tsx returns 0
    - grep -c "font-semibold\|font-bold\|font-medium" components/testimonials-section-simple.tsx returns 0
    - grep -c "font-black" components/testimonials-section-simple.tsx returns at least 4
    - Component still exports TestimonialsSectionSimple
  </acceptance_criteria>
  <done>TestimonialsSectionSimple renders with plain HTML divs, font-black weights only, no shadcn imports, no shadow violations. All tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Normalize ContactSectionSimple analytics + fix globals.css</name>
  <files>components/contact-section-simple.tsx, app/globals.css</files>
  <read_first>
    - components/contact-section-simple.tsx
    - app/globals.css
    - lib/analytics/track-event.ts
    - lib/analytics/types.ts
  </read_first>
  <action>
    **ContactSectionSimple normalization** (per Research Pitfall 1):

    1. Replace `import posthog from 'posthog-js'` with `import { trackEvent } from '@/lib/analytics/track-event'` and add `import { usePathname } from 'next/navigation'`.

    2. Add `const pathname = usePathname()` inside the component function body.

    3. Replace both `posthog.capture('contact_section_cta_clicked', ...)` calls:

    First CTA (line 26):
    ```tsx
    onClick={() => trackEvent('cta_click', {
      page: pathname || '/',
      element_id: '/devis-en-ligne',
      element_text: 'Demander une etude gratuite',
    })}
    ```

    Second CTA (line 35):
    ```tsx
    onClick={() => trackEvent('cta_click', {
      page: pathname || '/',
      element_id: '/contact',
      element_text: 'Nous contacter',
    })}
    ```

    **globals.css fix**:

    Find the `.gradient-hover` class. If it contains `shadow-xl`, replace with the same shadow values used by `.monolith-btn` (hard shadow pattern: `shadow-[5px_5px_0px_0px_rgba(0,0,0,1)]` or similar from the existing .monolith-btn definition). If `.gradient-hover` is not used anywhere in Phase 2 scope, simply remove the `shadow-xl` reference and replace with `shadow-none` or the monolith hard shadow equivalent.
  </action>
  <verify>
    <automated>cd /Users/alban/Developer/e2ivoip-front && grep -c "posthog.capture" components/contact-section-simple.tsx && grep -c "trackEvent" components/contact-section-simple.tsx && grep -c "shadow-xl" app/globals.css</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "posthog.capture" components/contact-section-simple.tsx returns 0
    - grep -c "trackEvent" components/contact-section-simple.tsx returns at least 2
    - grep -c "import.*trackEvent.*from.*@/lib/analytics/track-event" components/contact-section-simple.tsx returns 1
    - grep -c "usePathname" components/contact-section-simple.tsx returns at least 1
    - grep "shadow-xl" app/globals.css returns no matches in .gradient-hover class
    - Component still exports ContactSectionSimple
  </acceptance_criteria>
  <done>ContactSectionSimple uses trackEvent('cta_click') instead of direct posthog.capture. globals.css has no shadow-xl in .gradient-hover.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client component -> PostHog | Analytics events sent to external service |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | I (Information Disclosure) | trackEvent payloads | accept | Payloads contain only page path and button label text -- no PII, no form data. Already mitigated by Phase 1 design. |
| T-02-02 | T (Tampering) | Client-side analytics | accept | Analytics is write-only, client-side. Tampering with analytics does not affect site functionality or data integrity. |
</threat_model>

<verification>
- `grep -rn "hover:shadow-lg\|shadow-xl" components/testimonials-section-simple.tsx` returns empty
- `grep -rn "from.*@/components/ui/card\|from.*@/components/ui/badge" components/testimonials-section-simple.tsx` returns empty
- `grep -rn "font-semibold\|font-bold\|font-medium" components/testimonials-section-simple.tsx` returns empty
- `grep -rn "posthog.capture" components/contact-section-simple.tsx` returns empty
- `grep -rn "trackEvent" components/contact-section-simple.tsx` returns at least 2 matches
- `npm run build` completes without type errors in modified files
</verification>

<success_criteria>
All Monolithe Design System violations fixed in Phase 2 scope files. TestimonialsSectionSimple uses plain HTML with correct font weights. ContactSectionSimple uses standardized trackEvent analytics. No shadow-xl in globals.css gradient-hover.
</success_criteria>

<output>
After completion, create `.planning/phases/02-redesign-social-proof-conversion/02-01-SUMMARY.md`
</output>
