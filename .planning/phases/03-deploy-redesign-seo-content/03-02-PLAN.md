---
phase: 03-deploy-redesign-seo-content
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified: []
autonomous: false
requirements: [DSGN-02, DPLY-02, DPLY-03]

must_haves:
  truths:
    - "All production pages render with Monolithe 2026 design after merge"
    - "PostHog dashboard shows sessions from the production site"
    - "SEO content (JSON-LD, enriched pages) is live in production"
  artifacts: []
  key_links:
    - from: "main branch"
      to: "Vercel production"
      via: "auto-deploy on merge"
      pattern: "vercel.com"
    - from: "production site"
      to: "PostHog EU Cloud"
      via: "posthog-js SDK"
      pattern: "eu.i.posthog.com"
---

<objective>
Merge the PR to main and verify the production deployment is fully functional: all pages load with Monolithe 2026 design, PostHog captures events, and SEO content is live.

Purpose: This is the final verification step. Vercel auto-deploys on merge to main. We need to confirm everything works in production, not just in preview.

Output: Production site verified, PostHog collecting data, phase complete.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-deploy-redesign-seo-content/03-CONTEXT.md
@.planning/phases/03-deploy-redesign-seo-content/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Merge PR to main and wait for Vercel deployment</name>
  <files></files>
  <read_first>
    - .planning/phases/03-deploy-redesign-seo-content/03-01-SUMMARY.md (PR number and URL)
  </read_first>
  <action>
Step 1: Get the PR number from Plan 01's output:
```bash
PR_NUMBER=$(gh pr list --head dev --base main --json number --jq '.[0].number')
```

Step 2: Check that Vercel preview deployment on the PR is successful:
```bash
gh pr checks "$PR_NUMBER" --watch
```
Wait for all checks to pass (Vercel build + any CI).

Step 3: Merge the PR:
```bash
gh pr merge "$PR_NUMBER" --merge --delete-branch=false
```
Use `--merge` (not squash) to preserve the commit history. Do NOT delete the dev branch (we keep it for future work).

Step 4: Verify the merge:
```bash
git fetch origin main
git log --oneline origin/main -5
```
Confirm the dev commits are now on main.

Step 5: Wait ~60 seconds for Vercel production deployment to complete, then check:
```bash
vercel ls --prod 2>/dev/null || echo "Check https://vercel.com dashboard for deployment status"
```
  </action>
  <verify>
    <automated>gh pr list --head dev --base main --state merged --json number --jq 'length'</automated>
  </verify>
  <acceptance_criteria>
    - `gh pr list --head dev --base main --state merged` returns at least 1 PR
    - `git log --oneline origin/main -3` shows commits from the dev branch (e.g., commits containing "feat(02-03)" or "feat(01-01)")
    - Vercel production deployment completed (check via `vercel ls` or Vercel dashboard)
  </acceptance_criteria>
  <done>PR merged to main, Vercel production deployment triggered and completed</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify production deployment</name>
  <files></files>
  <action>
Present the following verification checklist to the user. The user must manually verify each item on the production site.
  </action>
  <what-built>
Production deployment of the complete Monolithe 2026 redesign with PostHog analytics, social proof, conversion optimization, and SEO content. Vercel auto-deployed after PR merge to main.
  </what-built>
  <how-to-verify>
1. Visit the production site (e2ivoip.fr or your Vercel production URL)
2. **Homepage check:**
   - Monolithe 2026 design visible (hard shadows, rounded-none, font-black typography)
   - Social proof section with testimonials visible between Services and Contact
   - 3CX Silver Partner badge visible
   - CTAs use .monolith-btn style (red background, hard shadow, uppercase)
3. **Product page check** (visit /telephonie-3cx):
   - CTA above the fold
   - Phone numbers are clickable tel: links
   - ContactSectionSimple at bottom (not a dead-end)
4. **Previously broken page** (visit /mobilite):
   - Full page loads (was previously notFound())
   - Has hero, features, CTA, phone links, contact section
5. **PostHog check:**
   - Open PostHog EU Cloud dashboard (eu.posthog.com)
   - Verify new sessions appear from the production domain
   - Click a CTA on the site, verify cta_click event appears
   - Click a phone number, verify phone_click event appears
6. **SEO check:**
   - View page source on /telephonie-3cx
   - Confirm JSON-LD script tag with Service schema is present
  </how-to-verify>
  <verify>User confirms production deployment is correct</verify>
  <done>User has approved the production deployment as functional and correct</done>
  <resume-signal>Type "approved" if production looks correct, or describe any issues found</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Vercel preview -> production | Deployment promotion |
| production site -> PostHog EU | Analytics data flow |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Denial of Service | Production deploy breaks site | mitigate | Vercel instant rollback available; preview deployment verified before merge |
| T-03-05 | Information Disclosure | PostHog leaking PII | accept | Cookieless mode (persistence: 'memory'), no PII collected, EU Cloud hosting |
</threat_model>

<verification>
1. PR is in "merged" state on GitHub
2. Production site loads all pages without errors
3. PostHog dashboard shows sessions from production domain
4. Human verified: Monolithe 2026 design, social proof, CTAs, phone links, SEO content all functional
</verification>

<success_criteria>
- Production site fully operational with Monolithe 2026 redesign
- PostHog collecting session and event data from production
- SEO content (JSON-LD, enriched pages) live in production
- Human verification confirms visual and functional correctness
</success_criteria>

<output>
After completion, create `.planning/phases/03-deploy-redesign-seo-content/03-02-SUMMARY.md`
</output>
